---
import Basic from '../layouts/Basic.astro';
import FragmentCard from '../components/Fragment.astro';
import fragments from '../data/fragments.json';
import '../styles/fragments.css';
---

<Basic>
  <section>
    <div class="container">
      <h1 class="title">A slightly less abridged representation of professional work — old work, pitches, one-offs, rejected ideas (fragments, if you will) — from 2005 to present.</h1>
    </div>
  </section>
  <div class="fragments">
    {
      fragments
        .sort((a, b) => b.year - a.year)
        .map((fragment) => (
          <FragmentCard
            name={fragment.name}
            type={fragment.type}
            year={String(fragment.year)}
            agency={fragment.agency}
            client={fragment.client}
            description={fragment.description}
            images={fragment.images}
            prototypes={fragment.prototypes}
          />
        ))
    }
  </div>
</Basic>
